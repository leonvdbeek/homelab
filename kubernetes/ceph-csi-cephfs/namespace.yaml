apiVersion: v1
kind: Namespace
metadata:
  name: ceph-csi-cephfs
  labels:
    # ceph-csi node DaemonSet needs hostPath + privileged for FUSE/kernel
    # mount helpers — same shape as csi-proxmox.
    pod-security.kubernetes.io/enforce: privileged
