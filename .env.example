# Copy to .env, fill in real values, then `source .env` before running playbooks.
# .env is gitignored.

# PVE root password — must match autopve storage-general.json's root-password
PVE_ROOT_PASSWORD=changeme

# NetBox
NETBOX_URL=http://192.168.4.73:8484
NETBOX_TOKEN=nbt_KEY.TOKEN_FROM_PROVISION_ENDPOINT
NETBOX_SUPERUSER_PASSWORD=changeme
NETBOX_BOOTSTRAP_TOKEN=0000000000000000000000000000000000000000
