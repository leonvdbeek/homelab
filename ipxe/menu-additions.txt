iseq ${menu_windows} 1 && item windows ${space} Windows ||
item --gap Custom:
item proxmox-auto ${space} Proxmox VE 9.1 (auto-install)

item --gap Tools:
iseq ${menu_utils} 1 && iseq ${platform} efi && item utils-efi ${space} Utilities (UEFI) ||
--

:proxmox-auto
echo Booting Proxmox VE 9.1 auto-install via kernel+initrd...
set base http://192.168.4.73:8181
imgfree
kernel ${base}/linux26 vga=791 video=vesafb:ywrap,mtrr ramdisk_size=16777216 rw quiet splash=silent proxmox-start-auto-installer initrd=initrd.magic
initrd ${base}/initrd.img
initrd ${base}/proxmox-ve_9.1-1-auto-from-http.iso /proxmox.iso
boot || goto error
goto main_menu
